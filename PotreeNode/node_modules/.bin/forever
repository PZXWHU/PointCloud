#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_forever@1.0.0@forever/bin/forever" "$@"
  ret=$?
else 
  node  "$basedir/../_forever@1.0.0@forever/bin/forever" "$@"
  ret=$?
fi
exit $ret
